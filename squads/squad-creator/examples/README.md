# Examples

Production outputs from squad-creator workflows are stored in:

- `.aiox/squad-runtime/squad-validations/` — validation reports per squad
- `.aiox/squad-runtime/create-squad/` — creation pipeline state per squad
- `.aiox/squad-runtime/validate-squad/` — validation pipeline state per squad

This directory serves as a discovery marker for the validation system.
