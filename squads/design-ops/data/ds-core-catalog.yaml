id: dops-ds-core-catalog
version: "1.0.0"
status: active
owner: design-ops
source_of_truth: docs

required_components:
  - id: button
    layer: core
    accessibility:
      keyboard_navigation: required
      focus_visible: required
      aria_contract: required
  - id: input
    layer: core
    accessibility:
      keyboard_navigation: required
      focus_visible: required
      aria_contract: required
  - id: dialog
    layer: core
    accessibility:
      keyboard_navigation: required
      focus_visible: required
      aria_contract: required

consumer_policy:
  design-pages: must_consume_project_component_contracts
  design-app: must_consume_project_component_contracts

