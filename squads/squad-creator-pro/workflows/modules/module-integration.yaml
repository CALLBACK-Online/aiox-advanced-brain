module:
  id: module-integration
  name: Integration Module
  purpose: Shared integration and publish-ready steps

  phases:
    - id: integration-register
      task: refresh-registry
      outputs:
        - "{registry_path}"

    - id: integration-project context governance
      task: create-documentation
      notes: "Persist local project docs contract in config/README for generated squads"
      outputs:
        - squads/{squad_name}/config.yaml
        - squads/{squad_name}/README.md
        - .aiox/squad-runtime/create-squad/{squad_name}/local project docs-level.yaml

    - id: integration-commands
      task: sync-ide-skills
      outputs:
        - skills/{slash_prefix}/{entry_agent}/SKILL.md

    - id: integration-codex-chief-skill
      task: sync-chief-codex-skill
      outputs:
        - skills/{entry_agent}/SKILL.md

    - id: integration-docs
      task: create-documentation
      outputs:
        - squads/{squad_name}/README.md
        - squads/{squad_name}/docs/
