# Examples

Production examples and test baselines live in `../test-cases/`.

This directory exists as a discovery marker for the validation system.
See `test-cases/` for 43 real extraction outputs across 19 task categories.
